#!/usr/bin/env perl
use strict;

BEGIN {
    print "If installed, try using the command line util pwgen", $/;
    print "pwgen -s 32", $/;
}

use Act::Util;
my $clear  = Act::Util::gen_password();
print "$clear\n";
